# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Environment variables - Keep secrets out of Git
.env
.env.local
.env.*.local

# Local Netlify folder
.netlify

backups/
Remove-ConsoleLogs.ps1

SSOT-PROJECT-FILES/

# Claude Code local settings (per-machine; do not commit)
.claude/settings.local.json

supabase/.temp/

# Imported content packages (external prototypes). Never commit -- these may
# contain non-ASCII source that would trip the ASCII pre-commit guard, and the
# prototype HTML must never ship in the build. ASCII-converted citation records
# are relocated to docs/ before use. Added 2026-06-10 (comparison page import).
_import/

# Security-audit working docs (2026-05/06). The repo is PUBLIC; these contain
# exposure analysis + the admin UUID and must never be committed. Kept locally
# for reference. Gitignoring also stops deploy.ps1's `git add .` from sweeping
# them into the public repo.
DEPLOYMENT_PLAN.md
DIAGNOSE_*.sql
SECURITY_ADVISOR_*.md
DRAFT_MIGRATION_*.sql
GENERATION_ANALYSIS.md
